<sitemapindex>
    <sitemap>
        <loc>{{ env('APP_URL') }}/</loc>
        <lastmod>{{ setting('admin.lastmode') }}</lastmod>

            <changefreq>monthly</changefreq>

            <priority>0.8</priority>
    </sitemap>
    <sitemap>
        <loc>{{ env('APP_URL') }}/catalog</loc>
        <lastmod>{{ setting('admin.lastmode') }}</lastmod>

            <changefreq>monthly</changefreq>

            <priority>0.8</priority>
    </sitemap>
    <sitemap>
        <loc>{{ env('APP_URL') }}/about</loc>
        <lastmod>{{ setting('admin.lastmode') }}</lastmod>

            <changefreq>monthly</changefreq>

            <priority>0.8</priority>
    </sitemap>
    <sitemap>
        <loc>{{ env('APP_URL') }}/news-page</loc>
        <lastmod>{{ setting('admin.lastmode') }}</lastmod>

            <changefreq>monthly</changefreq>

            <priority>0.8</priority>
    </sitemap>
    <sitemap>
        <loc>{{ env('APP_URL') }}/contact-page</loc>
        <lastmod>{{ setting('admin.lastmode') }}</lastmod>

            <changefreq>monthly</changefreq>

            <priority>0.8</priority>
    </sitemap>
    <sitemap>
        <loc>{{ env('APP_URL') }}/review-page</loc>
        <lastmod>{{ setting('admin.lastmode') }}</lastmod>

            <changefreq>monthly</changefreq>

            <priority>0.8</priority>
    </sitemap>
    <sitemap>
        <loc>{{ env('APP_URL') }}/site-map</loc>
        <lastmod>{{ setting('admin.lastmode') }}</lastmod>

            <changefreq>monthly</changefreq>

            <priority>0.8</priority>
    </sitemap>
</sitemapindex>
